%% Generated by eapi (DO NOT EDIT)
-ifndef(__LIBUSB_DRV_HRL__).
-define(__LIBUSB_DRV_HRL__,true).
-define(LIBUSB_CLASS_PER_INTERFACE,0).
-define(LIBUSB_CLASS_AUDIO,1).
-define(LIBUSB_CLASS_COMM,2).
-define(LIBUSB_CLASS_HID,3).
-define(LIBUSB_CLASS_PRINTER,7).
-define(LIBUSB_CLASS_PTP,6).
-define(LIBUSB_CLASS_MASS_STORAGE,8).
-define(LIBUSB_CLASS_HUB,9).
-define(LIBUSB_CLASS_DATA,10).
-define(LIBUSB_CLASS_VENDOR_SPEC,255).
-define(LIBUSB_DT_DEVICE,1).
-define(LIBUSB_DT_CONFIG,2).
-define(LIBUSB_DT_STRING,3).
-define(LIBUSB_DT_INTERFACE,4).
-define(LIBUSB_DT_ENDPOINT,5).
-define(LIBUSB_DT_HID,33).
-define(LIBUSB_DT_REPORT,34).
-define(LIBUSB_DT_PHYSICAL,35).
-define(LIBUSB_DT_HUB,41).
-define(LIBUSB_TRANSFER_TYPE_CONTROL,0).
-define(LIBUSB_TRANSFER_TYPE_ISOCHRONOUS,1).
-define(LIBUSB_TRANSFER_TYPE_BULK,2).
-define(LIBUSB_TRANSFER_TYPE_INTERRUPT,3).
-define(LIBUSB_REQUEST_GET_STATUS,0).
-define(LIBUSB_REQUEST_CLEAR_FEATURE,1).
-define(LIBUSB_REQUEST_SET_FEATURE,3).
-define(LIBUSB_REQUEST_SET_ADDRESS,5).
-define(LIBUSB_REQUEST_GET_DESCRIPTOR,6).
-define(LIBUSB_REQUEST_SET_DESCRIPTOR,7).
-define(LIBUSB_REQUEST_GET_CONFIGURATION,8).
-define(LIBUSB_REQUEST_SET_CONFIGURATION,9).
-define(LIBUSB_REQUEST_GET_INTERFACE,10).
-define(LIBUSB_REQUEST_SET_INTERFACE,11).
-define(LIBUSB_REQUEST_SYNCH_FRAME,12).
-define(LIBUSB_ENDPOINT_IN,128).
-define(LIBUSB_ENDPOINT_OUT,0).
-define(LIBUSB_REQUEST_TYPE_STANDARD,0).
-define(LIBUSB_REQUEST_TYPE_CLASS,32).
-define(LIBUSB_REQUEST_TYPE_VENDOR,64).
-define(LIBUSB_REQUEST_TYPE_RESERVED,96).
-define(LIBUSB_RECIPIENT_DEVICE,0).
-define(LIBUSB_RECIPIENT_INTERFACE,1).
-define(LIBUSB_RECIPIENT_ENDPOINT,2).
-define(LIBUSB_RECIPIENT_OTHER,3).
-define(LIBUSB_ISO_SYNC_TYPE_NONE,0).
-define(LIBUSB_ISO_SYNC_TYPE_ASYNC,32).
-define(LIBUSB_ISO_SYNC_TYPE_ADAPTIVE,64).
-define(LIBUSB_ISO_SYNC_TYPE_SYNC,96).
-record(libusb_device_descriptor,{
  bLength,
  bDescriptorType,
  bcdUSB,
  bDeviceClass,
  bDeviceSubClass,
  bDeviceProtocol,
  bMaxPacketSize0,
  idVendor,
  idProduct,
  bcdDevice,
  iManufacturer,
  iProduct,
  iSerialNumber,
  bNumConfigurations
}).
-record(libusb_endpoint_descriptor,{
  bLength,
  bDescriptorType,
  bEndpointAddress,
  bmAttributes,
  wMaxPacketSize,
  bInterval,
  bRefresh,
  bSynchAddress,
  extra,
  extra_length
}).
-record(libusb_interface_descriptor,{
  bLength,
  bDescriptorType,
  bInterfaceNumber,
  bAlternateSetting,
  bNumEndpoints,
  bInterfaceClass,
  bInterfaceSubClass,
  bInterfaceProtocol,
  iInterface,
  endpoint,
  extra,
  extra_length
}).
-record(libusb_interface,{
  altsetting,
  num_altsetting
}).
-record(libusb_config_descriptor,{
  bLength,
  bDescriptorType,
  wTotalLength,
  bNumInterfaces,
  bConfigurationValue,
  iConfiguration,
  bmAttributes,
  'MaxPower',
  interface,
  extra,
  extra_length
}).
-define(LIBUSB_CMD_SET_DEBUG,1).
-define(LIBUSB_CMD_GET_DEVICE_LIST,2).
-define(LIBUSB_CMD_GET_CONFIGURATION,3).
-define(LIBUSB_CMD_GET_DEVICE_DESCRIPTOR,4).
-define(LIBUSB_CMD_GET_ACTIVE_CONFIG_DESCRIPTOR,5).
-define(LIBUSB_CMD_GET_CONFIG_DESCRIPTOR,6).
-define(LIBUSB_CMD_GET_CONFIG_DESCRIPTOR_BY_VALUE,7).
-define(LIBUSB_CMD_GET_BUS_NUMBER,8).
-define(LIBUSB_CMD_GET_DEVICE_ADDRESS,9).
-define(LIBUSB_CMD_GET_MAX_PACKET_SIZE,10).
-define(LIBUSB_CMD_GET_MAX_ISO_PACKET_SIZE,11).
-define(LIBUSB_CMD_OPEN,12).
-define(LIBUSB_CMD_CLOSE,13).
-define(LIBUSB_CMD_SET_CONFIGURATION,14).
-define(LIBUSB_CMD_CLAIM_INTERFACE,15).
-define(LIBUSB_CMD_RELEASE_INTERFACE,16).
-define(LIBUSB_CMD_OPEN_DEVICE_WITH_VID_PID,17).
-define(LIBUSB_CMD_SET_INTERFACE_ALT_SETTING,18).
-define(LIBUSB_CMD_CLEAR_HALT,19).
-define(LIBUSB_CMD_RESET_DEVICE,20).
-define(LIBUSB_CMD_CONTROL_TRANSFER_WRITE,21).
-define(LIBUSB_CMD_CONTROL_TRANSFER_READ,22).
-define(LIBUSB_CMD_BULK_TRANSFER_WRITE,23).
-define(LIBUSB_CMD_BULK_TRANSFER_READ,24).
-define(LIBUSB_CMD_INTERRUPT_TRANSFER_WRITE,25).
-define(LIBUSB_CMD_INTERRUPT_TRANSFER_READ,26).
-define(LIBUSB_CMD_ISO_TRANSFER_WRITE,27).
-define(LIBUSB_CMD_ISO_TRANSFER_READ,28).
-define(LIBUSB_CMD_CANCEL_TRANSFER,29).
-define(LIBUSB_CMD_KERNEL_DRIVER_ACTIVE,30).
-define(LIBUSB_CMD_DETACH_KERNEL_DRIVER,31).
-define(LIBUSB_CMD_ATTACH_KERNEL_DRIVER,32).
-endif.
